import type { ThemeConfig } from "antd"

const theme: ThemeConfig = {
  token: {
    // Seed Token
    colorPrimary: "#1677FF",
    borderRadius: 12,

    // Alias Token
    colorBgContainer: "#f1f4f7",
  },
}

export default theme
